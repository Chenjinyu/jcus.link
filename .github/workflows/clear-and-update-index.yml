name: Legacy index workflow (disabled)

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Skip – no index pipeline for Next.js app
        run: echo "Index pipeline removed; no action required."
