name: Legacy eval workflow (disabled)

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Skip – no eval pipeline for Next.js app
        run: echo "Eval pipeline removed; no action required."
